import os
basedir = os.path.abspath(os.path.dirname(__file__))

class Config(object):
    # note: all-cap
    SECRET_KEY = os.environ.get('SECRET_KEY') or 'you-will-never-guess' 
    DEBUG = True
    # by default "http://localhost:6542", uncomment and fill in the below line to override. 
    # RESTFUL_API_ENDPOINT = ""

    #api key for google maps
    GOOGLE_API_KEY = "loTsaRanDOML3TTersAndN0MB3R2"

    # pushbullet details
    # step 1. create your own access token at https://www.pushbullet.com/#settings/account, fill in the key
    # step 2. create a channel at https://www.pushbullet.com/my-channel, fill in the channel name
    PUSHBULLET_KEY = ""
    PUSHBULLET_CHANNEL = ""
